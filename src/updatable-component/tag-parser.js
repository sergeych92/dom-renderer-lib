export class TagParser {
    constructor({id, attributes}) {

    }

    parse(element) {
        // go through attributes and make sure there are no duplicates (same name)
        return {}; // expression
    }
}